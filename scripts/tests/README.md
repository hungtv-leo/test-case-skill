# Skill self-tests

Unit tests for adapters, schema, and outcome helpers.

Run from the skill root:

```bash
pip install pytest jsonschema openpyxl
pytest .cursor/skills/self-test-cases/scripts/tests
```

Or, when developing the skill repo directly:

```bash
pytest scripts/tests
```
